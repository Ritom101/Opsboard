apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: opsboard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: opsboardacr2026.azurecr.io/opsboard-frontend:v1
        ports:
        - containerPort: 3000