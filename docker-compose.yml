services:
  backend:
    build: ./backend
    container_name: opsboard-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: opsboard-frontend
    ports:
      - "3000:80"